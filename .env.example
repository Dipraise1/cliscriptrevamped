# Solana Token Deployer Configuration

# Network (devnet, testnet, or mainnet-beta)
SOLANA_NETWORK=devnet

# Optional: Custom RPC URL
# If not specified, the default RPC URL for the selected network will be used
SOLANA_RPC_URL=https://api.devnet.solana.com

# Wallet path (path to your Solana keypair JSON file)
# Can also be provided via CLI using --wallet option
WALLET_PATH=./wallet.json

# API keys for image upload services (optional)
# Get an ImgBB API key from https://api.imgbb.com/
IMGBB_API_KEY=

# Get an NFT.Storage API key from https://nft.storage/
NFT_STORAGE_API_KEY= 